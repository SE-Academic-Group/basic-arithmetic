services:
 app:
   build:
     context: ./
     dockerfile: Dockerfile
   container_name: app
   restart: always
   ports:
     - 3000:3000
   volumes:
     - .:/app
     - /app/node_modules
   environment:
     VITE_APP_TITLE: Basic Calculator
   networks:
     - network
networks:
 network: